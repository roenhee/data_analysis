trino==0.336.0
duckdb>=1.1.0
pandas>=2.0.0
pyarrow>=15.0.0
# `screen_communities` 분석 전용. 다른 분석은 pandas/numpy 만 쓴다.
networkx>=3.0
pytest>=8.0.0
# api/ 의 FastAPI 레이어(main.py). starlette 버전에 따라 TestClient 의존성이
# httpx/httpx2 로 갈린다(1.3.1 은 httpx2) — 재현 가능하게 고정한다.
fastapi==0.141.1
starlette==1.3.1
uvicorn==0.52.1
httpx2==2.9.1
